# JLPT 단어 퀴즈 (니혼고래) — 프로젝트 규칙

순수 정적 사이트(HTML/CSS/JS, 빌드 없음), GitHub Pages 배포.
N1까지 단계별 확장 작업이 진행 중 — **전체 계획·결정 사항·상세 규칙의 원본은 `update.md`** (이 파일은 그 요약본. update.md §8 수정 시 여기도 함께 갱신).
아래 규칙에는 아직 구현 전인 항목(vocab-n3.js, example 필드 등)도 포함되어 있음 — 현재 코드와 계획의 차이는 update.md 참고.

## 데이터 규칙 (일본어 단어장: vocab.js, vocab-n3/n2/n1.js)

- 스키마 고정: `{ id, jpKana, jpKanji, krMeaning, note, example }` — 필드 추가·이름 변경 금지. 변경이 필요하면 update.md를 먼저 갱신.
- id는 레벨별 고정 범위: n5n4(vocab.js) 1~1495 / n3 10001~ / n2 20001~ / n1 30001~. **배포된 id는 변경·재사용 금지** (단어 삭제 시 결번으로 남김). 오답노트가 id에 의존함.
- 한자 없는 단어는 `jpKanji: ""` (빈 문자열). `"(한자 없음)"`은 신규 데이터에 사용 금지.
- 가타카나 외래어는 `jpKana`에 가타카나 그대로 (예: `jpKana: "エアコン"`).
- `krMeaning`: 대표 의미 1~3개, `". "`(마침표+공백)로 구분 (예: `"밝다. 환하다. 명랑하다"`). **일본어 표기와 한국어 뜻의 품사를 일치시킨다** — な형용사는 일본어도 기본형 〜だ로 기재하고 뜻은 "-하다"(`有名だ（ゆうめいだ）` → `"유명하다"`), 명사(する동사 어간 포함)는 だ 없이 명사형(`有名` → `"유명"`, `我慢` → `"참음. 인내"`). な형용사 예문 마커는 어간만 감싼다(`【正直】な人です`).
- `example`: 대상 단어의 표면형(활용형 포함)을 `【 】`로 정확히 1쌍 감싼 자연스러운 한 문장, 10~20자 내외 (예: `"駅で友だちに【会い】ました。"`). 해당 레벨 이하 어휘만 사용, 고유명사 금지. 자신 없으면 `""` (빈 값이면 文脈規定 출제에서 자동 제외).
- `note`: 평소 `""`. 검수 플래그(`"검수필요: <사유>"`) 전용이며 배포 전 비운다.
- 중복 기준은 `jpKana + jpKanji` 조합 — 하위 레벨에 이미 있는 단어는 상위 레벨 파일에 넣지 않는다. な형용사는 だ/な를 뗀 어간 기준으로도 비교 (`有名だ` ↔ 기존 `有名`은 같은 단어).

## 코드 규칙

- 빌드 도구·프레임워크·외부 라이브러리 도입 금지 (순수 정적 사이트 유지). 예외: 브라우저 내장 API(Web Speech 등)는 허용.
- 레벨·언어 추가는 매핑 테이블(`JA_LEVEL_VOCABS`, `EXAM_GENERATORS`, `LANGS`)에 항목을 추가하는 방식으로만. 분기문을 새로 늘리지 않는다.
- localStorage 키: `nihongorae-…-v<숫자>`. 저장 구조가 바뀌면 버전 숫자를 올리고 반드시 마이그레이션 코드를 함께 넣는다.
- URL 파라미터는 `lang` / `diff` / `level` / `cumulative` 4개만 사용, 각각 normalize 함수를 거친다 (잘못된 값 → 기본값).
- 출제 풀과 오답 보기 풀은 항상 같은 범위에서 뽑는다 (전역 `VOCAB` 직접 참조 금지).

## 진행 규칙

- 코드 변경 PR과 데이터 추가 PR은 분리. 레벨 데이터는 N3 → N2 → N1 순서, **한 레벨 = 한 PR**.
- 데이터 PR은 검증 스크립트 통과 필수: 스키마 일치 / id 범위·중복 / example 마커 1쌍·길이 / 하위 레벨과 단어 중복 없음(だ/な 뗀 어간 포함) / 품사 일치(jpKana가 だ로 끝나면 뜻도 전부 "-다").
- footer 버전: 기능 변경 시 minor, 데이터만 추가 시 patch.
- 데이터 생성·검수 5단계 파이프라인: 생성+자체 검수는 Codex(`codex exec` — ChatGPT 사용량 소모, **실행 전 사용자 확인 필수**) → 형식 검사는 검증 스크립트 → **교차 검수는 Claude (이종 모델 검증은 이 단계가 유일 — 생략 금지)** → 사람은 판정 불일치 항목 + 표본만 확인. 상세 절차는 update.md §2-3·§2-4. 스크립트는 `tools/`에 둔다.

## 작업 방식

- 애매하거나 계획에 없는 결정이 필요하면 **임의로 진행하지 말고 멈춰서 사용자에게 질문**한다.
- 구현은 사용자의 명시적 시작 지시 후에만. 계획 변경은 update.md에 먼저 반영한다.
