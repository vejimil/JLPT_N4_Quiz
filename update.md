# 일본어 N1까지 단계별 확장 — 업데이트 계획

> 작성일: 2026-06-11
> 확정된 방향:
> 1. 기존 vocab.js(1,495개)는 **N5·N4 묶음(기초)** 한 덩어리로 유지
> 2. N3·N2·N1 데이터는 **공개 JLPT 어휘 리스트**를 가져와 한국어 뜻으로 변환·검수
> 3. 출제 범위는 **기본: 선택 레벨만 + "하위 레벨 포함" 체크박스(누적 옵션)**
> 4. 레벨 선택은 **메인 퀴즈와 게임(빙고·산성비) 모두**에 적용
> 5. 출제 방식 강화(Phase 4): **Leitner식 간격 반복 오답노트 + 文脈規定(빈칸 예문) 모드 + 타이핑 입력 모드** — 이를 위해 스키마에 `example` 필드 추가

---

## 1. 현재 상태 요약

| 항목 | 현재 |
|---|---|
| 일본어 단어 데이터 | `vocab.js` — `VOCAB` 배열 1,495개 (N5+N4 혼합, 레벨 태그 없음) |
| 단어 스키마 | `{ id, jpKana, jpKanji, krMeaning, note }` (`id` 1~1495) |
| 퀴즈 | `index.html` + `app.js`, 언어(ja/fr/es)만 선택 가능 |
| 게임 | `game.html`(허브) → `bingo.html` / `acidrain.html` (`?lang=&diff=` URL 파라미터) |
| 저장소 | localStorage, 일본어 키 `nihongorae-jlpt-n4-v1` — 오답노트가 단어 `id`에 의존 |

핵심 제약: **오답노트(`wrongWordIds`)가 단어 `id`로 저장**되므로, 기존 단어의 id는 절대 바꾸면 안 되고 새 레벨의 id는 기존 범위와 절대 겹치면 안 된다.

---

## 2. 데이터 구조 변경

### 2-1. 레벨 체계 정의

```
n5n4  → 기존 vocab.js (기초, 1,495개)
n3    → 신규 (약 1,800개 예상)
n2    → 신규 (약 1,800개 예상)
n1    → 신규 (약 3,000개 이상 예상)
```

### 2-2. 파일 구성 (레벨별 파일 분리)

```
vocab.js        →  그대로 유지 (VOCAB, N5·N4 묶음)  ※ id 1~1495, 수정 금지
vocab-n3.js     →  신규: const VOCAB_N3 = [...]
vocab-n2.js     →  신규: const VOCAB_N2 = [...]
vocab-n1.js     →  신규: const VOCAB_N1 = [...]
```

- 스키마: `{ id, jpKana, jpKanji, krMeaning, note, example }` — 기존 5개 필드 + **`example`(예문) 필드 추가** (✅ Phase 4의 文脈規定 모드용으로 확정).
  - 한자가 없는 단어는 **`jpKanji: ""`(빈 문자열)** 사용 — 기존 데이터의 실제 규약 (190개 확인). 코드(app.js·bingo.js)는 `"(한자 없음)"` 문자열도 함께 분기하지만, 새 데이터에는 쓰지 않는다.
  - 가타카나 외래어는 기존 규약대로 `jpKana`에 가타카나 그대로 기재 (예: `jpKana: "エアコン", jpKanji: ""`).
  - **`example` 필드**: 대상 단어가 들어간 자연스러운 일본어 한 문장. 대상 단어의 표면형(활용형 포함)을 `【 】`로 감싼다 — 예: `example: "駅で友だちに【会い】ました。"`
    - 출제 시 `【…】`를 `＿＿`로 바꿔 빈칸 문제로, 정답 확인 후에는 마커만 벗겨 완성 문장으로 표시
    - 예문이 없는 단어는 `example: ""` — 文脈規定 모드 출제 대상에서 제외 (기존 1,495개는 일단 빈 값으로 두고 추후 보충 가능)
    - N3~N1 신규 데이터는 생성 단계에서 예문을 함께 생성한다 (§2-3)
- **id 범위를 레벨별로 고정 분리**해 충돌을 원천 차단:
  - n5n4: 1~1495 (기존), n3: 10001~, n2: 20001~, n1: 30001~
  - 이렇게 하면 누적 모드에서 배열을 합쳐도 id가 전역 유일하고, 오답노트도 그대로 동작한다.
- 빌드 단계가 없는 정적 사이트이므로 `<script src>` 추가만으로 로드. 전체 합산 약 1MB 수준이라 GitHub Pages에서 문제없지만, 모든 파일에 `defer` 속성을 붙이는 것을 권장.

### 2-3. 데이터 소싱 파이프라인 (N3·N2·N1)

1. **소스 후보** (공개 JLPT 어휘 리스트, 영어 뜻 기반):
   - Jonathan Waller의 JLPT Resources (tanos.co.uk) — 레벨별 어휘 CSV
   - GitHub의 jlpt-word-list 계열 공개 데이터셋 (elzup/jlpt-word-list 등, JSON)
   - JMdict(일→다국어 사전 데이터)로 뜻 보강
2. **변환**: 영어 뜻 → 한국어 뜻 생성 — **Codex가 수행** (Claude 토큰 절약, §2-4). 기존 vocab.js의 뜻 표기 스타일(`"오르다"`, `"밝다. 환하다. 명랑하다"`처럼 마침표로 복수 뜻 구분)에 맞추도록 §8-1 규칙을 생성 프롬프트에 포함한다.
   - ⚠️ **な형용사 변환 주의** (2026-06-12 소스 확인): 공개 리스트는 な형용사도 어간형으로 싣고 영어 뜻도 명사형이다 (elzup 리스트 실측: `正直 = honesty`, `単純 = simplicity`). 영어 뜻만 보고 변환하면 `"정직"`(명사형)이 나온다 → 형용동사로 판별되면 jpKana·jpKanji에 だ를 붙이고 뜻도 "-하다"로 변환하도록 프롬프트에 명시 (§8-1 품사 일치 규칙).
   - **예문 동시 생성**: 단어당 `example` 문장 1개를 같은 단계에서 생성 (§8-1의 예문 규칙 준수 — 해당 레벨 이하 어휘만 사용, 10~20자 내외, `【 】` 마커).
3. **중복 제거**: 공개 리스트는 레벨 간 단어가 겹치는 경우가 있음. `jpKana + jpKanji` 조합을 키로 해서
   - 기존 n5n4(1,495개)에 이미 있는 단어는 N3~N1에서 제외
   - N3·N2·N1 상호 간 중복은 더 낮은 레벨 쪽에만 남긴다.
4. **생성·검수**: 5단계 파이프라인 (✅ 확정 — 생성은 Codex, 교차 검수는 Claude로 역할 분담. 생성은 출력 토큰이 많아 비싸고 검수는 verdict만 출력해 저렴하다는 비대칭을 이용해 Claude 토큰을 절약):
   1. **Codex 생성**: 공개 리스트 → `krMeaning` + `example` 생성. §8-1 규칙(뜻 표기 스타일, 예문 마커 등)을 생성 프롬프트에 포함.
   2. **검증 스크립트** (무료·결정적): 스키마·마커·id 범위·중복 등 형식 오류를 전부 차단. 실패 항목은 재생성.
   3. **Codex 자체 검수**: 의미·예문 의심 항목 flag (verdict JSON만 출력 → 저렴).
      - ⚠️ 같은 모델 계열의 자체 검수는 무작위 실수는 잡지만 **체계적 오류(일관된 오역 패턴)는 못 잡는다** → 저렴한 1차 필터로만 취급.
   4. **Claude 교차 검수**: flag 전체 + 통과분 전수(또는 넉넉한 표본) 검토. **이종 모델 교차 검증은 이 단계가 유일하게 담당하므로 생략·과도한 표본 축소 금지.** 한국어 뜻 품질의 최종 판단 담당. 확신 없는 항목은 `note: "검수필요: <사유>"` 플래그.
   5. **사람 확인**: Codex/Claude **판정 불일치 항목** + 무작위 표본 5~10%를 네이버 사전과 직접 대조. 표본 오류율이 높으면(예: 3% 초과) 해당 레벨 전체 재검수. 두 AI의 판정이 일치한 항목은 신뢰도가 높아 사람 부담이 줄어든다.
   - ⚠️ 공통 한계: AI는 네이버 사전에 실제 접속하는 게 아니라 자체 지식으로 대조함. 네이버 사전 자동 수집(스크래핑)은 약관 문제로 하지 않는다.
   - 레벨당 한 파일씩 PR로 나눠서 진행 (N1은 다의어가 많아 검수 부담이 가장 큼).

### 2-4. 생성·검수 자동화 파이프라인 (Codex CLI + Claude) — ✅ 자동화 가능 확인됨

- **환경** (2026-06-11 Codespace에서 확인): `codex` CLI v0.139.0 설치·ChatGPT 계정 로그인 완료. 비대화형 모드 `codex exec "프롬프트"`로 스크립트에서 호출 가능. Node v24 사용 가능.
- **역할 분담**: 생성 + 자체 검수 = Codex (ChatGPT 사용량) / 교차 검수 = Claude (Claude 토큰, 입력 위주라 생성 대비 저렴) / 형식 검사 = 검증 스크립트 (무료) / 최종 판정 = 사람
- **절차** (Phase 3에서 Claude Code가 오케스트레이션, 모든 스크립트는 `tools/`에):
  1. **생성 루프**: 공개 리스트를 50~100단어 단위 JSON 청크로 분할(`tools/chunk-vocab.js`) → 청크마다 `codex exec` 호출해 `krMeaning`+`example` 생성. 프롬프트에 §8-1 규칙과 출력 형식(스키마 JSON만) 고정.
  2. **검증 스크립트**(`tools/validate-vocab.js`): 스키마·`【 】` 마커·id 범위·중복 검사. 실패 항목은 해당 청크 재생성.
  3. **자체 검수 루프**: 청크마다 `codex exec` 호출. 기준은 jpKana/jpKanji 표기 정확성, krMeaning이 네이버 일본어사전 대표 뜻과 부합, example의 문법·용법·레벨 초과 어휘. 출력은 `{ "id": …, "verdict": "ok" | "flag", "reason": "…" }` JSON 배열만 — 형식을 벗어나면 해당 청크 재시도.
  4. **Claude 교차 검수**: Claude Code 세션에서 배치로 검토 (flag 전체 + 통과분 전수 또는 넉넉한 표본). 결과를 3번 결과와 합쳐 flag 항목에 `note: "검수필요: <사유>"` 자동 기입 (`tools/merge-review.js`).
  5. **사람 검수 리포트 생성**: Codex/Claude **판정 불일치 항목** + 무작위 표본 5~10%를 마크다운 표로 출력. 단어별 네이버 사전 검색 링크(`https://ja.dict.naver.com/#/search?query=<단어>`) 포함 — **링크 제공만, 자동 수집(스크래핑) 금지**. な형용사는 だ를 뗀 어간으로 링크 생성 (사전 표제어는 어간형 `曖昧 [名・形動]`이라 `曖昧だ`로는 검색이 안 될 수 있음)
- **유의사항**:
  - `codex exec` 호출은 사용자 ChatGPT 요금제의 사용량을 소모 — 생성과 자체 검수 두 번 돌므로 N3 1,800단어 기준 약 36~72회 호출. **실행 전 사용자 확인 후 진행.**
  - "네이버 사전 기준"은 검토 기준 지시일 뿐, AI가 실제 사전에 접속하는 것이 아님 → 사람 확인 단계는 생략 불가.
  - 이종 모델 교차 검증은 4번 Claude 단계가 유일하게 담당 (Codex 자체 검수는 1차 필터) → 이 단계 생략 금지.

---

## 3. 퀴즈(app.js / index.html) 변경

### 3-1. index.html

- [x] 일본어 선택 시에만 보이는 **레벨 선택 UI** 추가 (설정 패널 내):
  - 버튼 4개: `N5·N4(기초)` / `N3` / `N2` / `N1`
  - 체크박스 1개: `하위 레벨 단어 포함` (예: N2 선택 + 체크 → N5~N2 전체 출제)
  - 프랑스어/스페인어 선택 시에는 이 UI를 숨김
- [x] `<title>`과 `h1#app-title`의 "JLPT N4 단어 퀴즈" → 레벨 반영 동적 타이틀 (예: "JLPT N3 단어 퀴즈")
- [x] `<script>` 추가: `vocab-n3.js`, `vocab-n2.js`, `vocab-n1.js` (vocab.js 다음, app.js 이전)
- [x] footer 버전 표기 갱신 (ver1.1.4 → 다음 버전)

### 3-2. app.js

- [x] 상태 추가: `currentJaLevel` (`"n5n4" | "n3" | "n2" | "n1"`, 기본 `"n5n4"`), `includeLowerLevels` (boolean)
- [x] 레벨 → 배열 매핑 테이블 추가:
  ```js
  const JA_LEVEL_VOCABS = {
    n5n4: () => VOCAB,
    n3:   () => VOCAB_N3,
    n2:   () => VOCAB_N2,
    n1:   () => VOCAB_N1,
  };
  const JA_LEVEL_ORDER = ["n5n4", "n3", "n2", "n1"];
  ```
- [x] `getCurrentVocab()` 수정 (app.js:53): 일본어일 때 선택 레벨(+누적 체크 시 하위 레벨 합산) 배열 반환
- [x] **기존 버그성 코드 수정 — 필수**: `buildQuestionForWord()`의 오답 보기 풀이 `VOCAB`을 직접 참조함 (app.js:324 `let others = VOCAB.filter(...)`). 이대로면 N1 시험의 보기가 N5 단어로 채워짐. `getCurrentVocab()` 기반으로 변경해서 **보기(오답 선택지)도 같은 출제 범위에서** 나오게 해야 한다.
- [x] 저장소 분리 — **경험치는 통합, 오답노트는 레벨별** (✅ 확정):
  - 경험치(XP) 키: `nihongorae-ja-xp-v1` — `{ totalQuestions, totalCorrect }`만 저장. 일본어 내 모든 레벨이 공유하므로 어느 레벨을 풀어도 니혼고래는 같이 자란다.
  - 오답노트 키 (레벨별): `nihongorae-jlpt-n5n4-v1`, `nihongorae-jlpt-n3-v1`, `nihongorae-jlpt-n2-v1`, `nihongorae-jlpt-n1-v1`
  - 오답노트 저장 구조는 **Phase 1부터 Leitner 호환 형태로** 설계: `{ wrongWords: [{ id, box, lastSeen }] }` — Phase 1에서는 전부 `box: 1`로만 저장해 동작은 기존과 동일하게 유지하고, Phase 4에서 승급/졸업 로직만 얹는다 (이중 마이그레이션 방지)
  - 프랑스어/스페인어 키는 기존 그대로 (변경 없음)
  - **마이그레이션**: 첫 로드 시 기존 키 `nihongorae-jlpt-n4-v1`이 있고 새 키가 없으면, `totalQuestions`/`totalCorrect`는 XP 키로, `wrongWordIds`는 `{ id, box: 1, lastSeen: null }` 형태로 변환해 `n5n4` 키로 복사 (기존 사용자의 레벨·오답노트 보존)
  - 오답노트 복습(`startNewExam(true)`)은 현재 레벨의 키에서 읽으므로 자동으로 레벨별 오답노트가 됨
  - 구현 주의: 현재 `globalStats` 객체 하나에 XP와 오답이 합쳐져 있으므로(app.js:78), 저장/로드 함수(app.js:105, 129)를 두 키로 읽고 쓰도록 분리해야 함
- [x] 레벨 변경 시 `loadGlobalStats()` + `updateCharacterPanel()` 재호출 (언어 버튼과 동일한 패턴, app.js:848 참고)
- [x] `LANGS.ja.title`(app.js:9)의 "JLPT N4" 하드코딩 제거, 레벨 반영

### 3-3. 누적 모드 출제 시 동작 정의

- 출제 풀과 오답 보기 풀 모두 "선택 레벨 + 하위 레벨 합산" 배열 사용
- 오답노트 저장은 **현재 선택된 레벨의 키**에 기록 (누적 모드에서 N5 단어를 틀려도 N2 키에 저장 — id가 전역 유일하므로 단어 조회는 문제없음)

---

## 4. 게임(game.js / bingo.js / acidrain.js) 변경

### 4-1. game.html / game.js (허브)

- [x] 현재 흐름: 언어 선택 → 난이도(EASY/NORMAL/HARD) 모핑 → 게임 페이지로 이동
- [x] **일본어 선택 시에만** 레벨 선택 단계 추가 — **방식 A로 확정 (✅)**: 난이도 화면에 레벨 버튼 한 줄(N5·N4 / N3 / N2 / N1) + "하위 레벨 포함" 체크 추가. 모핑 애니메이션 통일(방식 B)은 추후 별도 작업.
  - 레벨 라벨 이미지 에셋이 없어 `Button.png` 배경 + 텍스트 라벨로 렌더(난이도 버튼 위쪽 배치). 표시는 CSS `body.difficulty-mode[data-lang="ja"] .ja-levels`로만 제어(모핑 미적용). 데이터 0개 레벨은 회색 비활성화(`refreshLevelAvailability`).
- [x] 게임 페이지 이동 URL에 파라미터 추가 (`buildGameUrl`, 일본어일 때만 level/cumulative 부착):
  `bingo.html?lang=ja&diff=normal&level=n3&cumulative=1`
- [x] "하위 레벨 포함" 여부도 파라미터로 전달 (`cumulative=0|1`)

### 4-2. bingo.js / acidrain.js

- [x] `qsParam('level', 'n5n4')`, `qsParam('cumulative', '0')` 읽기 + normalize 함수 추가 (`normalizeJaLevel`/`normalizeCumulative`, 기존 `normalizeLang`/`normalizeDiff` 패턴)
- [x] `getPairs(lang)` → `getPairs(lang, level, cumulative)`로 확장: 일본어일 때 `getJaVocab(level, cumulative)`로 레벨 배열(들)에서 pool 구성 (출제·오답 보기 풀 동일 범위)
- [x] 뒤로가기/재시작 시 URL 파라미터 정리 로직에 `level`, `cumulative`도 추가 (back 버튼에서 `lang`/`diff` 지우는 곳)
- [x] `bingo.html`, `acidrain.html`, `game.html`에 `vocab-n3.js`, `vocab-n2.js`, `vocab-n1.js` 스크립트 추가 (game.html은 레벨 비활성화 판정을 위해 `vocab.js`도 함께 로드)

---

## 5. 단계별 실행 계획

### Phase 1 — 구조 작업 (데이터 없이도 진행 가능)
1. `vocab-n3.js` / `vocab-n2.js` / `vocab-n1.js`를 소량의 샘플 단어(레벨당 10~20개, id 범위 적용, `example` 필드 포함)로 생성
2. app.js / index.html 레벨 선택 + 누적 옵션 + storageKey 분리·마이그레이션 구현
3. 오답 보기 풀 버그(app.js:324) 수정
4. 수동 테스트: 레벨 전환, 누적 모드, 오답노트 레벨별 분리, 기존 localStorage 마이그레이션

### Phase 2 — 게임 적용
1. game.js 허브에 레벨 선택 + URL 파라미터 전달
2. bingo.js / acidrain.js 레벨 필터 적용
3. 수동 테스트: 레벨·누적 파라미터별 게임 동작

### Phase 3 — 데이터 채우기
1. 공개 리스트 수집 → 한국어 뜻 + 예문(`example`) 생성(Codex, §2-4) → 중복 제거 → 레벨별 파일 생성 (N3 → N2 → N1 순서 권장)
2. 레벨당 5단계 생성·검수 파이프라인 실행 (Codex 생성·자체 검수 → Claude 교차 검수 → 사람 확인, §2-3·§2-4)
3. 검수 완료된 레벨부터 순차 배포 (UI에서 데이터 없는 레벨은 비활성화 표시)

> **진행 상황 (2026-06)**
> - 파이프라인 스크립트 `tools/` 구현 완료 (`prepare-source` → `gen-codex`+`prompts/` → `merge-gen` → `validate-vocab` → `review-report`). 상세·소스 URL은 `tools/README.md`.
> - **접사·조수사(～ 표기) 제외 결정** (사용자 확인): 단독 단어가 아니므로 단어→뜻 퀴즈에서 뺀다. `prepare-source`가 ～ 항목을 자동 제외. (語形成 모드는 Phase 4 backlog)
> - **N3 완료**: elzup CSV 2,139행 → 중복·접사 제거 후 **2,058단어**(id 10001~12061, 결번 일부) → `vocab-n3.js` 확정. 검증 0 errors / 21 warnings(짧은 예문).
> - **N2 완료**: elzup CSV 1,747행 → 중복·접사(165개) 제거 후 **1,555단어**(id 20001~21556, ×기호 1건 제거 결번) → `vocab-n2.js` 확정. 검증 0 errors / 3 warnings.
>   - 기존 15개 샘플(N3 10001~10015 / N2 20001~20015)은 placeholder였고 전체 데이터로 교체됨(사용자 확인).
>   - **계획 대비 차이**: §2-3의 *Codex 자체 검수(3단계)*는 생략하고 결정적 검증 스크립트 + Claude 전수 교차검수로 품질 게이트를 대체(사용량 절약). 사람 확인 리포트는 `tools/out/<level>-review.md`(네이버 링크 포함).
> - **N1 완료**: elzup CSV 2,698행 → 중복(10) + 접사(36) 제거 후 **2,653단어**(id 30001~32653, 결번 없음, 54청크) → `vocab-n1.js` 확정. 프롬프트 그대로 재사용(개선 불필요). 검증 0 errors / 10 warnings(짧은 예문). 교차검수: 동음이의 66그룹 전수 + 무작위 표본 133개 거의 전부 정확, `興業`(흥행→흥업·예문 교체)·`ガレージ`(영어 잔존 제거) 2건 수정. N1 placeholder 샘플(30001~)은 전체 데이터로 교체됨.
>   - **검증기 보강**: だ送り仮名 부사 `未だ`·`甚だ`가 품사 검사에서 な형용사로 오탐되어 `tools/validate-vocab.js`에 예외 집합(`DA_NOT_NA`) 추가. N3·N2 회귀 0 errors 확인.
> - footer ver1.3.3 (N3·N2·N1 데이터 patch).
> - **Phase 3 데이터 채우기 완료** (N3·N2·N1 모두 배포). 다음은 Phase 4(출제 방식 강화: Leitner 오답노트·文脈規定·타이핑 입력).

### Phase 4 — 출제 방식 강화 (✅ 채택 확정: 1·2·3번)

실제 JLPT 문자·어휘 시험의 출제 형식(漢字読み/表記/文脈規定/言い換え類義/用法/語形成) 중 현재 앱은 漢字読み·表記 계열만 커버하고 있음. 학습과학 근거(능동 회상 + 간격 반복)와 실전 충실도를 높이는 단계.

1. **Leitner식 간격 반복 오답노트**
   - 오답 복습에서 맞히면 `box` 승급(1→5, 출제 빈도 감소), 틀리면 `box: 1`로 강등, box 5에서 맞히면 졸업(목록에서 제거)
   - 복습 출제 시 낮은 box·오래된 `lastSeen` 단어를 우선 출제
   - 저장 구조는 Phase 1에서 이미 Leitner 호환으로 만들어두므로(§3-2) 마이그레이션 불필요
2. **文脈規定(빈칸 예문) 모드** — 새 출제 모드 `contextFill`
   - `example`이 있는 단어만 출제 대상 (한자 모드 게이팅과 같은 패턴, app.js:578 참고)
   - 문제: `【 】`를 `＿＿`로 바꾼 문장 제시 + "빈칸에 들어갈 단어는?" / 보기: 같은 출제 범위에서 4개 + 정답 (사전형 표기)
   - 정답 확인 후 마커를 벗긴 완성 문장을 함께 표시
   - 빈칸은 활용형, 보기는 사전형이라는 불일치는 알려진 단순화로 허용 (실제 JLPT는 보기도 빈칸에 맞는 형태)
3. **타이핑 입력 모드**
   - 한국어 뜻 → 히라가나 직접 입력, `jpKana` 일치로 채점 (객관식보다 강한 기억 형성 — 산출 연습)
   - 알려진 한계: 같은 뜻의 다른 단어(동의어)를 입력하면 오답 처리됨 — 문제에 한자 힌트 표시 등으로 보완 검토
   - 설정 패널에 답안 방식 선택(객관식/입력) 추가
4. **후보 (이번 미채택, backlog)**
   - 言い換え類義 모드 — 유의어 그룹 데이터 필요
   - 用法 모드 — 단어당 올바른 문장 1 + 오용 문장 3 필요 (제작 비용 최대)
   - 듣기 모드 — 브라우저 내장 Web Speech API 사용 (외부 라이브러리 불필요, §8-2 규칙에 부합)

---

## 6. 리스크 / 주의사항

| 리스크 | 대응 |
|---|---|
| 단어 id 충돌 → 오답노트 오염 | 레벨별 id 범위 고정 분리 (10001~/20001~/30001~), 기존 id 불변 |
| 기존 사용자 데이터 유실 | 구 키 → `n5n4` 키 1회 마이그레이션 코드 |
| 보기(오답)가 다른 레벨에서 출제 | app.js:324 `VOCAB` 직접 참조 제거 (Phase 1에서 필수 수정) |
| 공개 리스트의 한국어 뜻 품질 | 기계 변환 + 레벨별 사람 검수, 기존 뜻 표기 스타일 준수 |
| N1 다의어·긴 뜻으로 보기 버튼이 길어짐 | 뜻은 대표 의미 1~2개로 제한, CSS 줄바꿈 확인 |
| 전체 단어 파일 로드 용량(~1MB) | `defer` 적용, 문제 시 레벨별 지연 로드 검토 (정적 사이트라 후순위) |
| 예문 품질 (문법 오류, 레벨 초과 어휘, 부자연스러운 문장) | 예문 생성 규칙(§8-1) + AI 교차 검수에 예문 포함(§2-3), 예문 없는 단어는 文脈規定 출제에서 제외되므로 불확실하면 빈 값으로 두는 게 안전 |
| `【 】` 마커 누락·깨짐으로 빈칸 생성 실패 | 데이터 검증 스크립트로 example 비어있지 않으면 마커 정확히 1쌍 존재하는지 검사 (PR 머지 전 필수) |

## 7. 결정 사항 (✅ 모두 확정)

1. **니혼고래 경험치**: 통합 유지 — XP는 일본어 공용 키 하나, 오답노트만 레벨별 분리 (§3-2에 반영)
2. **게임 허브 레벨 선택 UI**: 방식 A (난이도 화면에 레벨 버튼 한 줄) (§4-1에 반영)
3. **N3~N1 데이터 생성·검수**: 5단계 파이프라인 — Codex 생성 → 검증 스크립트 → Codex 자체 검수 → Claude 교차 검수 → 사람 확인 (§2-3·§2-4에 반영, 2026-06-11 역할 변경: 생성을 Claude→Codex로 이관해 Claude 토큰 절약, 교차 검증은 Claude 검수 단계가 담당).
   - 참고: AI는 네이버 사전에 실제 접속해 대조하는 게 아니라 자체 지식으로 검토하므로, 판정 불일치 항목 + 무작위 표본의 사람 확인 단계는 생략하지 않는다.
4. **일관성 규칙 마련**: §8에 정리하고, 핵심 규칙은 `CLAUDE.md`로 생성 완료 — 모든 Claude Code 세션에 자동 적용됨. **§8이 원본**이며, §8을 고치면 CLAUDE.md도 함께 갱신한다.
5. **출제 방식 강화 (Phase 4)**: Leitner 오답노트 + 文脈規定 모드 + 타이핑 입력 모드 채택 (§5 Phase 4). 文脈規定을 위해 스키마에 `example` 필드를 지금 추가하고(§2-2), N3~N1 데이터 생성 시 예문을 함께 만든다. 言い換え類義·用法·듣기 모드는 backlog.

---

## 8. 작업 규칙 (일관성 유지)

> 이 섹션이 규칙의 원본이며, 핵심 요약본이 `CLAUDE.md`에 있다 (모든 Claude Code 세션에 자동 로드). 이 섹션을 수정하면 CLAUDE.md도 함께 갱신할 것.

### 8-1. 데이터 규칙

- **스키마 고정**: `{ id, jpKana, jpKanji, krMeaning, note, example }` — 필드 추가·이름 변경 금지. 변경이 필요하면 이 문서를 먼저 갱신하고 진행.
- **id 규칙**: 레벨별 고정 범위(n5n4 1~1495 / n3 10001~ / n2 20001~ / n1 30001~). 한 번 배포된 id는 변경·재사용 금지. 단어를 삭제해도 그 id는 결번으로 남긴다.
- **한자 없는 단어**: `jpKanji: ""` (빈 문자열). `"(한자 없음)"`은 새 데이터에 사용 금지.
- **가타카나 외래어**: `jpKana`에 가타카나 그대로 기재.
- **krMeaning 표기**:
  - 대표 의미 1~3개까지만, 복수 의미는 `". "`(마침표+공백)로 구분 — 예: `"밝다. 환하다. 명랑하다"`
  - **일본어 표기와 한국어 뜻의 품사를 일치시킨다** (양쪽 다 기본형 또는 양쪽 다 명사형):
    - な형용사는 일본어도 기본형 「〜だ」로 기재(jpKana·jpKanji 모두) + 뜻은 "-하다/-이다": `有名だ（ゆうめいだ）` → `"유명하다"`, `曖昧だ` → `"애매하다. 모호하다"`
    - 명사(する동사의 어간 포함)는 だ 없이 + 뜻은 명사형: `有名` → `"유명"`, `我慢` → `"참음. 인내"`
    - 같은 한자어가 명사·형용사 양쪽으로 쓰이면 해당 레벨에서 더 대표적인 용법 하나로 싣는다
  - 괄호 보충 설명은 꼭 필요할 때만 짧게
- **note 필드**: 평소 빈 문자열. 검수 파이프라인에서 `"검수필요"` 플래그로만 사용하고, 배포 전 비운다.
- **example 필드 (예문)**:
  - 대상 단어가 들어간 자연스러운 일본어 한 문장, 10~20자 내외. 대상 단어의 표면형(활용형 포함)을 `【 】`로 정확히 1쌍 감싼다 — 예: `"駅で友だちに【会い】ました。"`
  - な형용사(〜だ로 기재)는 예문에서 **어간만** 감싼다 — 예: `"彼はとても【正直】な人です。"` (단어는 `正直だ`로 기재)
  - 예문에 쓰는 다른 어휘는 **해당 단어의 레벨 이하**로 제한 (N3 단어의 예문에 N1 어휘 금지)
  - 고유명사(인명·지명·상품명) 사용 금지, 보통체(です/ます체 권장) 통일
  - 자신 있게 만들 수 없으면 `""`(빈 값)로 둔다 — 빈 값이면 文脈規定 출제에서 자동 제외되므로 틀린 예문보다 안전
- **중복 기준**: `jpKana + jpKanji` 조합. 하위 레벨에 이미 있는 단어는 상위 레벨 파일에 넣지 않는다.
  - な형용사는 **だ/な를 뗀 어간 기준으로도** 비교한다 — 신규 `有名だ`와 기존 어간형 `有名`·〜な형 `きれいな`(vocab.js 기존 표기)는 키가 달라도 같은 단어다.

### 8-2. 코드 규칙

- 빌드 도구·프레임워크·외부 라이브러리 도입 금지 — 순수 정적 사이트(HTML/CSS/JS) 유지.
- 레벨·언어 추가는 매핑 테이블(`JA_LEVEL_VOCABS`, `EXAM_GENERATORS`, `LANGS`)에 항목을 추가하는 방식으로만. 분기문을 새로 늘리지 않는다.
- localStorage 키 네이밍: `nihongorae-…-v<숫자>`. 저장 구조가 바뀌면 버전 숫자를 올리고 반드시 마이그레이션 코드를 함께 넣는다.
- URL 파라미터는 `lang` / `diff` / `level` / `cumulative` 4개만 사용하고, 각각 normalize 함수를 거친다 (잘못된 값 → 기본값).
- 출제 풀과 오답 보기 풀은 항상 같은 범위에서 뽑는다 (전역 `VOCAB` 직접 참조 금지).

### 8-3. 진행 규칙

- 코드 변경 PR과 데이터 추가 PR은 분리한다.
- 레벨 데이터는 N3 → N2 → N1 순서로, **한 레벨 = 한 PR**.
- 데이터 PR 머지 전 **검증 스크립트**를 통과해야 한다: 스키마 필드 일치, id가 레벨 범위 내·중복 없음, `jpKana` 비어있지 않음, `example`이 비어있지 않으면 `【 】` 마커 정확히 1쌍 + 10~20자 내외, 하위 레벨과 `jpKana+jpKanji` 중복 없음(だ/な를 뗀 어간 기준 포함), **품사 일치**(jpKana가 だ로 끝나면 krMeaning의 모든 의미도 "-다"로 끝나야 함).
- 데이터 없는 레벨은 UI에서 비활성화(회색) 처리해 빈 시험이 생기지 않게 한다.
- footer 버전: 기능 변경 시 minor(1.2.0), 데이터만 추가 시 patch(1.2.1) 올림.
- 각 Phase 완료 시 §5의 수동 테스트 항목을 확인한 뒤 머지한다.